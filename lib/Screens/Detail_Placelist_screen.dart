import 'package:flutter/material.dart';

class DetailPlaceList extends StatelessWidget {
  const DetailPlaceList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
